/**
 * 
 */
package com.ora.idm.connector;

/**
 * This is the enum of the user attributes.
 */
public enum UserAttributeEnum {
	LAN_ID, FIRST_NAME, LAST_NAME, OWNER, DEFAULT_GROUP, PASSWORD, DATA, UACC;
}
